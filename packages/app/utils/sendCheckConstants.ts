/**
 * The message that must be signed to prove ownership of the ephemeral key.
 * Used by both client (to sign) and server (to verify).
 */
export const CHECK_NOTE_ACCESS_MESSAGE = 'Send Check Note Access'
